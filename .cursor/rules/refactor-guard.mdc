---
description: 重构防打转规则 — 修改已有控制流/执行顺序时先列约束再动手
globs: autoc/core/**/*.py,autoc/agents/**/*.py
alwaysApply: false
---

# 重构防打转规则

## 适用场景

当你要 **修改已有控制流或执行顺序**（如调换函数调用顺序、重构 if/else 分支、改变数据处理管道）时，必须先执行下面的约束检查流程。

**不适用**于纯增量改动（新增关键词、新增分支、新增函数），这类改动可以直接做。

## 动手前：列出所有约束

在修改代码之前，先回答以下问题：

1. **这段代码必须同时满足哪些约束？** 列出所有已知的不变量（invariant），包括：
   - 下游消费者对输出的依赖（如 task.verification_steps 需要包含 L4 补全的步骤）
   - 质量门控的判断依据（如 L2 应只基于 LLM 原始产出判断，不被自动补全稀释）
   - 安全约束（如白名单校验、沙箱隔离）
   - 性能约束（如避免 ReAct 循环空转）

2. **这些约束之间有没有表面矛盾？** 如果有，找到同时满足所有约束的方案再动手。常见模式：
   - **快照模式**：先保存原始状态，执行变更，再用快照做判断（如 L4/L2 快照隔离）
   - **分层模式**：在不同层面分别满足不同约束
   - **标记模式**：给自动生成的内容加标记，与人工内容区分

3. **有没有现有测试锁定这些约束？** 如果没有，先加测试再改代码。相关测试文件：
   - PM 验证逻辑：`tests/test_pm_validation.py`
   - 环境修复逻辑：`tests/test_env_repair.py`

## 禁止的模式

- **只解决一个约束而破坏另一个**：如"为了降低误判把 L4 提前"但忽略了"L4 会稀释 L2 判断"
- **写死代码假装解决问题**：如 `original_steps = list(task.verification_steps)` 在 L4 还没执行时做快照，实际是空操作
- **改了又改回**：如果发现上一轮的改动需要回退，停下来重新分析约束，不要简单回退

## 示例：L4/L2 执行顺序

约束 A：L2 质量判断不能被 L4 自动补全的 `test -f` 稀释
约束 B：task 需要 L4 补全的步骤供下游 Dev/Tester 使用

正确方案（快照模式）：
```python
original_steps = list(task.verification_steps)  # 快照
self._auto_complete_verification(task)           # L4 补全（task 获得完整步骤）
# L2 用 original_steps 判断（不受 L4 影响）
```

错误方案 1：L4 在 L2 前执行且 L2 看补全后的列表 → 违反约束 A
错误方案 2：L4 在 L2 后执行 → 违反约束 B（下游拿不到完整步骤时 L2 已判定）
