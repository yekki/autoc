---
description: Git 工作流纪律 — 进场检查脏状态 + 离场必须提交
globs: 
alwaysApply: true
---

# Git 工作流纪律

## 进场：开始任务前检查工作区

每次会话开始修改代码前，先跑 `git status --short`。如果发现 **未提交的改动**：

1. **告知用户**：列出脏文件数量和大致范围
2. **建议处理**：先 commit / stash 现有改动，再开始新任务
3. **不要直接在脏工作区上叠加修改**，除非用户明确同意

## 离场：完成任务后必须提交

完成代码修改后，**主动提议 commit**：

1. 用 `git diff --stat` 汇总改动范围
2. 向用户提议 commit，给出建议的 commit message
3. 等用户确认后执行 commit
4. 如果用户拒绝或跳过，**明确提醒**：工作区有未提交的改动

## 禁止的行为

- **禁止**：改完代码不提 commit 就结束会话
- **禁止**：在已有大量脏文件的工作区上继续堆改动而不告知用户
- **禁止**：把多个不相关功能混在一个 commit 里（超过 15 个文件时必须分组）
