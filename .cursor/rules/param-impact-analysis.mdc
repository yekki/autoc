---
description: 修改函数参数/签名前必须做影响分析，防止"加了参数忘传递"的典型缺陷
globs:
  - "autoc/**/*.py"
  - "tests/**/*.py"
alwaysApply: false
---

# 参数修改影响分析

## 触发条件

当你要做以下任何一项操作时，**必须先做影响分析**：

1. 给函数/方法**新增参数**
2. 修改参数的**默认值**或**语义**
3. 修改函数的**返回值结构**
4. 重命名参数或函数

## 影响分析步骤

### 1. 搜索所有调用方

用 Grep 搜索函数名，找到**全部调用点**（包括间接调用、测试、路由层、CLI 入口）：

```
必查范围：
- 直接调用方（同模块 + 跨模块）
- 转发/包装函数（如 A 调 B 调 C，改 C 必须检查 B 是否需要透传）
- 测试文件中的调用
- CLI / API 路由层的调用
```

### 2. 逐一确认

每个调用点判断：
- 是否需要传入新参数？
- 包装函数是否需要新增同名参数并透传？
- 默认值变更是否影响现有调用方的预期行为？

### 3. 典型反面案例

```python
# ❌ 给 _run_single_case 加了 use_alarm 参数
# 但包装函数 _run_repeated_case 内部调用时没有透传
# → 并行模式下 worker 线程仍触发 SIGALRM，行为未定义

def _run_repeated_case(...):
    cr = _run_single_case(case, ..., on_progress=on_progress)  # 漏传 use_alarm

# ✅ 正确做法：包装函数也加参数并透传
def _run_repeated_case(..., use_alarm=True):
    cr = _run_single_case(case, ..., on_progress=on_progress, use_alarm=use_alarm)
```

## 禁止

- **禁止**：只改被调函数签名就完事，不检查调用链
- **禁止**：假设"其他调用方用默认值就行"而不逐一确认
